services:
  alledrogo.post.api:
    image: alledrogo.post.api
    container_name: post.api
    build:
      context: .
      dockerfile: src/AlledrogO.Post/AlledrogO.Post.Api/Dockerfile
    ports:
      - "5000:8080"
