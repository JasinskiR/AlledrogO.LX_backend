# AlledrogO.LX_backend

## How to run backend
Go to the root of the backend project and run the following command:
```powershell
docker-compose up -d --build
```
Make sure that docker is running on your machine.